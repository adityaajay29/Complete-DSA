#include <iostream>
#include <unordered_map>
using namespace std;

// prob: to find the pattern's index in the string
// approach : we use sliding window  technique
// but to save computation time, we cpmare the pattern and window only if there hash value maches

