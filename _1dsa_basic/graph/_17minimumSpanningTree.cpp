#include <iostream>
#include <vector>
#include <queue>
using namespace std;

// if we can convert a weighted graph (v vertices and e edges) in a tree
// with exactly v vertices and v-1 edges and all the vertices are reachable by all the vertices
// then it is called spanning tree
// if the sum of the weights in a possible spanning tree is minimum, it is called mini. spanning tree

